import { useGameStore } from '../store/useGameStore';

export const useControls = () => {
  const updateControls = () => {
    // This hook will contain the logic from controlsUpdate()
  };

  return { updateControls };
};
